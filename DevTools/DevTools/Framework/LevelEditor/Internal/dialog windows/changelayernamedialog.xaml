<Window
	x:Class="BrawlerEditor.ChangeLayerNameDialog" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	Title="Add New Layer"
	Height="110"
	Width="280"
	ResizeMode="NoResize"
	WindowStartupLocation="CenterScreen" xmlns:brawlereditor="clr-namespace:BrawlerEditor">
	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition
				Width="80" />
			<ColumnDefinition />
		</Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition
				Height="10" />
			<RowDefinition
				Height="*" />
			<RowDefinition
				Height="35" />
		</Grid.RowDefinitions>
		<StackPanel
			Grid.Column="0"
			Grid.Row="1">
			<Label
				Content="Layer Name:"
				VerticalAlignment="Center"
				HorizontalAlignment="Right" />
		</StackPanel>
		<StackPanel
			Grid.Column="1"
			Grid.Row="1">
			<TextBox
				Name="changeLayerNameTextBox"
				VerticalAlignment="Center"
				Width="180"
				HorizontalAlignment="Left" />
		</StackPanel>
		<Button
			Grid.Column="0"
			Grid.Row="2"
			Grid.ColumnSpan="2"
			Content="OK"
			Width="40"
			Height="25"
			HorizontalAlignment="Center"
			Click="Button_Click" />
	</Grid>
</Window>
